import UIKit
import SwiftUI

@main
struct BringApp: App {
//    @StateObject private var modelData = ModelData()

    var body: some Scene {
        WindowGroup {
            ContentView()
//                .environmentObject(modelData)
        }
    }
}
